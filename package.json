{
  "name": "gamehall",
  "version": "0.1.3",
  "private": true,
  "main": "electron/main.cjs",
  "description": "GameHall, a local-first planner for personal game nights.",
  "scripts": {
    "dev": "next dev --hostname 127.0.0.1",
    "desktop:dev": "concurrently -k \"npm:dev\" \"npm:desktop:open\"",
    "desktop:open": "wait-on http://127.0.0.1:3000 && electron .",
    "build": "next build",
    "desktop:build": "npm run build && node scripts/prepare-desktop.mjs && electron-builder --win",
    "desktop:dir": "npm run build && node scripts/prepare-desktop.mjs && electron-builder --win --dir",
    "start": "next start --hostname 127.0.0.1",
    "lint": "eslint src tests electron scripts *.ts *.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@hookform/resolvers": "latest",
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "@vitest/coverage-v8": "^4.1.10",
    "better-sqlite3": "^12.11.1",
    "concurrently": "^10.0.3",
    "drizzle-orm": "latest",
    "electron": "^43.1.0",
    "electron-builder": "^26.15.3",
    "eslint": "latest",
    "eslint-config-next": "latest",
    "lucide-react": "latest",
    "next": "^16.2.9",
    "postcss": "^8.5.10",
    "react": "latest",
    "react-dom": "latest",
    "react-hook-form": "latest",
    "resend": "latest",
    "typescript": "latest",
    "vitest": "^4.1.10",
    "wait-on": "^9.0.10",
    "zod": "latest"
  },
  "overrides": {
    "postcss": "^8.5.10"
  },
  "build": {
    "appId": "com.gamehall.desktop",
    "productName": "GameHall",
    "directories": {
      "app": "electron",
      "output": "release"
    },
    "npmRebuild": false,
    "compression": "maximum",
    "electronLanguages": [
      "en-US"
    ],
    "files": [
      "main.cjs",
      "package.json"
    ],
    "extraResources": [
      {
        "from": ".next/standalone",
        "to": "app-server"
      },
      {
        "from": ".desktop-runtime",
        "to": "runtime"
      },
      {
        "from": "build/icon.png",
        "to": "icon.png"
      }
    ],
    "win": {
      "target": "nsis",
      "icon": "build/icon.png",
      "artifactName": "GameHall-Setup-${version}.${ext}"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true
    }
  }
}
